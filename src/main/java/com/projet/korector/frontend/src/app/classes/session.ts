export class Session {
    private id : number;
    private name:string;
    //private projects : Array<Project>

    constructor(id : number, name : string)
    {
        this.id=id;
        this.name=name;
    }

}
